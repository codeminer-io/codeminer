% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_snomed_ct_uk_monolith.R
\name{get_snomed_ct_uk_monolith}
\alias{get_snomed_ct_uk_monolith}
\title{Get SNOMED CT UK Monolith Edition from NHS TRUD}
\usage{
get_snomed_ct_uk_monolith(
  dir_path = tempdir(),
  release = "latest",
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dir_path}{Directory path to download to. Defaults to \code{tempdir()}.}

\item{release}{Character string specifying which release to download. Can be:
\itemize{
\item \code{"latest"} (default) - Downloads the most recent release
\item A specific release identifier string (e.g., \code{"uk_sct2mo_41.3.0_20251217000001Z.zip"})
}}

\item{overwrite}{Logical. If \code{TRUE}, re-downloads and overwrites existing
files. Default is \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses informational messages. Default
is \code{FALSE}.}
}
\value{
File path to the downloaded SNOMED CT UK Monolith zip file
(invisibly).
}
\description{
Downloads the SNOMED CT UK Monolith Edition (item 1799) from NHS TRUD.
}
\details{
This function requires a valid NHS TRUD API key set as an environment
variable \code{TRUD_API_KEY}. You must also be subscribed to item 1799 on the
NHS TRUD website.

By default, the latest release is downloaded. The function checks if the
target zip file already exists and skips the download if so (unless
\code{overwrite = TRUE}).

The returned zip file path can be passed directly to
\code{\link[=read_snomed_ct_uk_monolith]{read_snomed_ct_uk_monolith()}}, which will extract the contents on-demand.
}
\examples{
\dontrun{
# Download latest SNOMED CT UK Monolith to tempdir
snomed_zip <- get_snomed_ct_uk_monolith()

# Get available releases
metadata <- trud::get_item_metadata(1799, release_scope = "all")
available_releases <- purrr::map_chr(metadata$releases, "id")

# Download specific release
snomed_zip <- get_snomed_ct_uk_monolith(
  release = "uk_sct2mo_41.3.0_20251217000001Z.zip"
)

# Download to specific directory for persistent storage
snomed_zip <- get_snomed_ct_uk_monolith(
  dir_path = "~/data/snomed",
  overwrite = TRUE
)

# Read the downloaded data (extracts on-demand)
snomed <- read_snomed_ct_uk_monolith(snomed_zip)
}
}
\seealso{
\itemize{
\item \code{\link[=read_snomed_ct_uk_monolith]{read_snomed_ct_uk_monolith()}} to read the downloaded data into R
\item \code{\link[trud:download_item]{trud::download_item()}} for the underlying download function
\item \code{\link[trud:get_item_metadata]{trud::get_item_metadata()}} to retrieve available release identifiers
}
}
