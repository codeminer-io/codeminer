% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{missing_codes_warning}
\alias{missing_codes_warning}
\title{Warn about missing codes in a table}
\usage{
missing_codes_warning(
  missing_codes,
  table_meta,
  table_type = c("lookup", "mapping", "relationship"),
  max_show = 10
)
}
\arguments{
\item{missing_codes}{Character vector of all missing codes (untruncated).}

\item{table_meta}{Data frame of metadata for the table the codes were
expected to be found in.}

\item{table_type}{Type of table the codes were expected to be found in. Must
be one of 'relationship', 'lookup', or 'mapping'.}

\item{max_show}{Maximum number of missing codes to display. Defaults to 10.}
}
\value{
Invisibly returns \code{missing_codes}.
}
\description{
Emits a truncated warning listing codes that were not found in a given table.
The warning carries the missing codes and table context as structured data
and can be handled programmatically.
}
\details{
\subsection{Handling missing-code warnings}{

This function signals a warning of class \code{"codeminer_missing_codes"}. The
warning object contains the following fields:
\itemize{
\item \code{missing_codes}: a character vector of all missing codes (untruncated)
\item \code{table_type}: the type of table the codes were expected to be found in
(e.g. \code{"lookup"}, \code{"mapping"}, \code{"relationship"})
\item \code{table_meta}: metadata for the table that was queried (including table
name, version, code type, and source)
}

You can intercept and act on these warnings using \code{\link[=withCallingHandlers]{withCallingHandlers()}}
(see examples). This allows you to collect missing inputs without printing a
warning, while still preserving full context about where the lookup failed.
}
}
\examples{
# Set up a temporary dummy database
temp_db <- tempfile(fileext = ".duckdb")
create_dummy_database(temp_db)
missing_codes <- table_type <- table_meta <- NULL

# Capture missing codes from waning using `withCallingHandlers()`
withCallingHandlers(
  {
    codes <- CODES(c("foo", "bar", "E10"), code_type = "ICD-10")
  },
  codeminer_missing_codes = function(w) {
    missing_codes <<- w$missing_codes
    table_type <<- w$table_type
    table_meta <<- w$table_meta
    invokeRestart("muffleWarning")
  }
)

# Recognised codes
codes

# Unrecognised codes and related context
missing_codes
table_type
table_meta
}
\keyword{internal}
