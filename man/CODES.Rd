% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codes.R
\name{CODES}
\alias{CODES}
\alias{CODES_LIKE}
\title{Look up descriptions for clinical codes}
\usage{
CODES(
  ...,
  type = getOption("codeminer.code_type"),
  lookup_version = getOption("codeminer.lookup_version", default = "latest"),
  preferred_description_only = TRUE,
  col_filters = "default"
)

CODES_LIKE(
  pattern,
  type = getOption("codeminer.code_type"),
  lookup_version = getOption("codeminer.lookup_version", default = "latest"),
  preferred_description_only = TRUE,
  col_filters = "default"
)
}
\arguments{
\item{...}{Codes to look up. Can be:
\itemize{
\item Character vectors: \code{CODES("E10", "E11", type = "ICD-10")}
\item \code{||} separated strings: \code{CODES("E10 || E11", type = "ICD-10")}
\item Data frame with code/description/code_type columns: \code{CODES(my_df)}
\item Mixed: \code{CODES("E10", my_vector, "E13 || E14", type = "ICD-10")}
}

Special values: \code{"all"} returns all codes; empty input returns empty
codelist.

Comments can be added with \verb{<< >>} syntax: \code{"E10 << Type 1 diabetes >>"}.}

\item{type}{character. Type of clinical code system to be searched. Optional
if input is a data frame with code_type column. Depends on what is
available in the lookup tables. See \code{\link[=add_lookup_table]{add_lookup_table()}} on how to add new
lookup tables. This can also be configured through the
\code{codeminer.code_type} option.}

\item{lookup_version}{character. Version of the lookup table to use. Default:
\code{"latest"}. Can be configured through the \code{codeminer.lookup_version}
option.}

\item{preferred_description_only}{logical. If \code{TRUE}, only returns the
preferred description for each code. Default: \code{FALSE}.}

\item{col_filters}{Column filters for the tables this query touches. One
of:
\itemize{
\item \code{"default"} (default): apply session-pinned filters
(\code{\link[=codeminer_set_col_filters]{codeminer_set_col_filters()}}) where set, else the metadata-defined
default filters.
\item \code{NULL} (or \code{NA}): no filtering for any table this query touches.
\item A table-keyed list — the shape returned by \code{\link[=get_col_filters]{get_col_filters()}} — with
top-level names \code{lookup} / \code{relationship} / \code{mapping}, keyed by code
type (or \code{"from > to"} pair for mappings), e.g.
\code{list(lookup = list("SNOMED CT" = list(active_concept = "1")))}.
Each table entry \emph{replaces} that table's pinned/default filters
wholesale; tables the list does not name keep their pins/defaults.
\code{NA} as a table entry un-filters that one table. The filters reach
every table the query touches (e.g. both the mapping table and the
target lookup in \code{\link[=MAP]{MAP()}}).
}

To tweak one column while keeping a table's other default filters,
amend \code{\link[=get_col_filters]{get_col_filters()}} output and pass it back. Entries that match
no registered table or column trigger a warning.}

\item{pattern}{a regular expression to search for}
}
\value{
A \code{codeminer_codelist} object (tibble) containing the codes and their
descriptions
}
\description{
Returns a codelist with descriptions for the codes of interest. Supports
flexible input: character vectors, \code{||} separated strings, or data frames.
}
\details{
\code{CODES_LIKE} searches for codes that match a given regular
expression. The matching is case-insensitive.
}
\examples{
# Set up a temporary dummy database
temp_db <- tempfile(fileext = ".duckdb")
create_dummy_database(temp_db)

# Multiple arguments
CODES("E10", "E11", type = "ICD-10")

# With comments
CODES("E10 << Type 1 diabetes >>", type = "ICD-10")

# || separated string
CODES("E10 || E11", type = "ICD-10")

# Splice operator
my_codes <- c("E10", "E11")
CODES(!!!my_codes, type = "ICD-10")

# Data frame input
df <- data.frame(
  code = c("E10", "E11"),
  description = c("Type 1", "Type 2"),
  code_type = c("ICD-10", "ICD-10")
)
CODES(df)
CODES_LIKE("^E1", type = "ICD-10")
}
\seealso{
\code{\link[=add_lookup_table]{add_lookup_table()}} for adding new lookup tables to the database.

Other Clinical code lookups and mappings:
\code{\link[=CODES_ALL_CHUNK]{CODES_ALL_CHUNK()}},
\code{\link[=CODES_LIKE_CHUNK]{CODES_LIKE_CHUNK()}},
\code{\link[=DESCRIPTION_CHUNK]{DESCRIPTION_CHUNK()}},
\code{\link[=MAP]{MAP()}},
\code{\link[=get_lookup_table]{get_lookup_table()}},
\code{\link[=get_mapping_table]{get_mapping_table()}},
\code{\link[=get_relationship_table]{get_relationship_table()}},
\code{\link[=get_relationship_tree]{get_relationship_tree()}}
}
\concept{Clinical code lookups and mappings}
