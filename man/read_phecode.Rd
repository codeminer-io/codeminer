% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_phecode.R
\name{read_phecode}
\alias{read_phecode}
\title{Read Phecode 1.2 files into lookup and mapping tables}
\usage{
read_phecode(
  lkp_path = NULL,
  icd10_map_path = NULL,
  icd9_map_path = NULL,
  version = "v1.2",
  source = "https://phewascatalog.org/phecodes"
)
}
\arguments{
\item{lkp_path}{Path to the Phecode 1.2 definitions CSV file. If \code{NULL},
this table is skipped.}

\item{icd10_map_path}{Path to the Phecode 1.2 to ICD-10 (beta) mapping CSV
file. If \code{NULL}, this table is skipped.}

\item{icd9_map_path}{Path to the Phecode 1.2 to ICD-9 mapping \code{.rda} file.
If \code{NULL}, this table is skipped.}

\item{version}{Character string for the version label (default: \code{"v1.2"}).}

\item{source}{Character string for the data source URL or description.}
}
\value{
A named list containing any of the following elements (depending on
which paths are provided):
\itemize{
\item \code{phecode_lkp}: list with \code{lookup} → \code{list(table, metadata)}
\item \code{icd10_phecode}: list with \code{mapping} → \code{list(table, metadata)}
\item \code{icd9_phecode}: list with \code{mapping} → \code{list(table, metadata)}
}
}
\description{
Reads the Phecode 1.2 definitions file and/or ICD mapping files and returns
structured lookup and mapping tables for use with CodeMiner.
}
\examples{
result <- read_phecode(
  lkp_path = dummy_phecode_lkp_path(),
  icd10_map_path = dummy_icd10_phecode_map_path()
)
result$phecode_lkp$lookup$table
result$icd10_phecode$mapping$table

}
\seealso{
\code{\link[=add_phecode]{add_phecode()}}, \code{\link[=get_phecode]{get_phecode()}}
}
