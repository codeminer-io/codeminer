% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/description.R
\name{DESCRIPTION_CHUNK}
\alias{DESCRIPTION_CHUNK}
\title{Chunked, bounded DESCRIPTION search}
\usage{
DESCRIPTION_CHUNK(
  pattern,
  type = getOption("codeminer.code_type"),
  cursor = 0L,
  batch_size = getOption("codeminer.chunk_batch_size", default = 200000L),
  total_rows = NULL,
  accumulated_so_far = 0L,
  max_rows = getOption("codeminer.max_leaf_rows", default = 30000L),
  max_chunk_matches = getOption("codeminer.max_chunk_matches", default = 20000L),
  lookup_version = getOption("codeminer.lookup_version", default = "latest"),
  ignore_case = TRUE,
  preferred_description_only = TRUE,
  col_filters = "default"
)
}
\arguments{
\item{pattern}{The description to search for. See \code{\link[stringr:str_detect]{stringr::str_detect()}} for
details.}

\item{type}{character. Type of clinical code system to be searched. Optional
if input is a data frame with code_type column. Depends on what is
available in the lookup tables. See \code{\link[=add_lookup_table]{add_lookup_table()}} on how to add new
lookup tables. This can also be configured through the
\code{codeminer.code_type} option.}

\item{cursor}{Integer. \code{rowid} to start this chunk's scan from. \code{0} for
the first call.}

\item{batch_size}{Integer. Number of \code{rowid}s to scan in this call.
Defaults to \code{getOption("codeminer.chunk_batch_size", default = 2000)}.}

\item{total_rows}{Integer or \code{NULL}. The underlying table's total row
count. Pass \code{NULL} on the first call (it will be resolved and returned);
pass the previously-returned value on subsequent calls to skip
re-resolving it.}

\item{accumulated_so_far}{Integer. Total matched rows found by this leaf's
previous chunks (before this one). \code{0} for the first call. Used to check
\code{max_rows} cumulatively across the whole chunked fetch, not just this one
call.}

\item{max_rows}{Integer. Ceiling on this leaf's cumulative matched rows
across all chunks. Aborts with class \code{codeminer_max_leaf_rows_exceeded}
if exceeded. Defaults to
\code{getOption("codeminer.max_leaf_rows", default = 30000)}.}

\item{max_chunk_matches}{Integer. Ceiling on the number of codes matched
\emph{within this single chunk}, checked before the matches are expanded via
\code{\link[=CODES]{CODES()}}. Guards against a chunk whose scan range happens to match
densely - \code{\link[=CODES]{CODES()}}'s cost scales with match count, not rows scanned, so
this bounds a single call's worst case independently of \code{batch_size}.
Aborts with class \code{codeminer_chunk_match_limit_exceeded} if exceeded.
Defaults to \code{getOption("codeminer.max_chunk_matches", default = 20000)}.}

\item{lookup_version}{character. Version of the lookup table to use. Default:
\code{"latest"}. Can be configured through the \code{codeminer.lookup_version}
option.}

\item{ignore_case}{If \code{TRUE} (default), ignore case in \code{description}.}

\item{preferred_description_only}{\code{logical}. If \code{TRUE} (default), return only
preferred descriptions.}

\item{col_filters}{Column filters to apply. See \code{\link[=CODES]{CODES()}} for details.}
}
\value{
A list with \code{result} (a \code{codeminer_codelist} of this chunk's
matches), \code{next_cursor} (integer, pass back as \code{cursor} next call),
\code{total_rows} (integer, pass back as \code{total_rows} next call), and
\code{exhausted} (logical — \code{TRUE} once \code{next_cursor} has covered the whole
table).
}
\description{
Bounded variant of \code{\link[=DESCRIPTION]{DESCRIPTION()}} for callers that need to keep any single
call's work small (e.g. to stay under a network-layer request timeout),
rather than materialising every match in one unbounded scan.
}
\details{
Each call scans only the \code{rowid} range \verb{[cursor, cursor + batch_size)} of
the underlying lookup table for matches, expands those matches to full
rows via \code{\link[=CODES]{CODES()}}, and returns a cursor for the next chunk. Callers
should keep calling with the returned \code{next_cursor} (and the same
\code{total_rows} and \code{accumulated_so_far}, updated each time) until
\code{exhausted} is \code{TRUE}.
}
\examples{
create_dummy_database()
chunk <- DESCRIPTION_CHUNK("cyst", type = "ICD-10", batch_size = 100)
chunk$result
chunk$exhausted
}
\seealso{
Other Clinical code lookups and mappings:
\code{\link[=CODES]{CODES()}},
\code{\link[=CODES_ALL_CHUNK]{CODES_ALL_CHUNK()}},
\code{\link[=CODES_LIKE_CHUNK]{CODES_LIKE_CHUNK()}},
\code{\link[=MAP]{MAP()}},
\code{\link[=get_lookup_table]{get_lookup_table()}},
\code{\link[=get_mapping_table]{get_mapping_table()}},
\code{\link[=get_relationship_table]{get_relationship_table()}},
\code{\link[=get_relationship_tree]{get_relationship_tree()}}
}
\concept{Clinical code lookups and mappings}
