% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{attributes}
\alias{attributes}
\alias{ATTRIBUTES_FOR}
\alias{HAS_ATTRIBUTES}
\title{Get attributes or codes with attributes}
\usage{
ATTRIBUTES_FOR(
  ...,
  type = getOption("codeminer.code_type"),
  lookup_version = getOption("codeminer.lookup_version", default = "latest"),
  relationship_version = getOption("codeminer.relationship_version", default = "latest"),
  relationship_types = NULL,
  preferred_description_only = TRUE,
  max_nodes = getOption("codeminer.max_traversal_nodes", default = 20000L),
  col_filters = "default"
)

HAS_ATTRIBUTES(
  ...,
  type = getOption("codeminer.code_type"),
  lookup_version = getOption("codeminer.lookup_version", default = "latest"),
  relationship_version = getOption("codeminer.relationship_version", default = "latest"),
  relationship_types = NULL,
  preferred_description_only = TRUE,
  max_nodes = getOption("codeminer.max_traversal_nodes", default = 20000L),
  col_filters = "default"
)
}
\arguments{
\item{...}{Codes to start from. Supports flexible input like \code{\link[=CODES]{CODES()}}.}

\item{type}{Code type (character).}

\item{lookup_version}{Lookup table version (character).}

\item{relationship_version}{Relationship table version (character).}

\item{relationship_types}{Relationship types to filter by. Can be a character
vector, a \code{codeminer_codelist} (e.g. from \code{\link[=CODES]{CODES()}}), or a data frame with
a \code{code} column. Supports \verb{<<description>>} comments. If \code{NULL} (default),
all relationship types are included.}

\item{preferred_description_only}{Logical. If \code{TRUE}, return only preferred
descriptions.}

\item{max_nodes}{Integer. Ceiling on the number of codes the traversal can
accumulate before aborting (checked after each generation of the
traversal, before the next one runs). Defaults to
\code{getOption("codeminer.max_traversal_nodes", default = 20000)}.}

\item{col_filters}{Column filters to apply. See \code{\link[=CODES]{CODES()}} for details.}
}
\value{
A data frame of codes and descriptions
}
\description{
These functions traverse the relationship graph to find attributes for codes
or codes that have specific attributes.
}
\details{
\itemize{
\item \code{ATTRIBUTES_FOR()} returns attribute codes for the supplied codes
\item \code{HAS_ATTRIBUTES()} returns codes that have the specified attribute codes
}
}
\examples{
create_dummy_database()
# ATTRIBUTES_FOR returns attributes for codes
# HAS_ATTRIBUTES returns codes that have the specified attributes
}
\seealso{
Other Code relationships:
\code{\link{parent_child_retrieval}},
\code{\link{relationship_types}}
}
\concept{Code relationships}
