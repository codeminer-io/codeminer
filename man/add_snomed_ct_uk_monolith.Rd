% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_snomed_ct_uk_monolith.R
\name{add_snomed_ct_uk_monolith}
\alias{add_snomed_ct_uk_monolith}
\title{Add SNOMED CT UK Monolith tables to CodeMiner database}
\usage{
add_snomed_ct_uk_monolith(
  path = get_snomed_ct_uk_monolith(),
  tables = c("sct_lookup", "sct_relationship", "sct_icd10", "sct_opcs4"),
  version = NULL,
  source = "https://isd.digital.nhs.uk/trud/",
  .icd10_refset_id = "999002271000000101",
  .opcs4_refset_id = "999002321000000109"
)
}
\arguments{
\item{path}{Path to the SNOMED CT UK Monolith Edition. This can be either:
\itemize{
\item A \strong{zip file} (e.g., from \code{\link[=get_snomed_ct_uk_monolith]{get_snomed_ct_uk_monolith()}})
\item An \strong{unzipped directory} containing the subdirectories
\code{Snapshot/Terminology} and \code{Snapshot/Refset}
}

Default uses \code{\link[=get_snomed_ct_uk_monolith]{get_snomed_ct_uk_monolith()}} to download the latest release.}

\item{tables}{Character vector of table names to read and add. Available tables:
\itemize{
\item Lookup table: \code{"sct_lookup"}
\item Relationship table: \code{"sct_relationship"}
\item Mapping tables: \code{"sct_icd10"}, \code{"sct_opcs4"}
}

By default, adds all tables.}

\item{version}{Character string specifying the version label for the SNOMED
CT release. If not provided, it will be derived from the zip filename or
folder name.}

\item{source}{Character string specifying the source URL or description.
Defaults to the NHS TRUD website.}

\item{.icd10_refset_id}{Character string. The SNOMED CT Concept ID
identifying the specific Reference Set (Refset) used for ICD-10 mappings.
Defaults to \code{"999002271000000101"}. This is an advanced parameter.}

\item{.opcs4_refset_id}{Character string. The SNOMED CT Concept ID
identifying the specific Reference Set (Refset) used for OPCS-4 mappings.
Defaults to \code{"999002321000000109"}. This is an advanced parameter.}
}
\value{
Invisibly returns the result from \code{\link[=read_snomed_ct_uk_monolith]{read_snomed_ct_uk_monolith()}} (a
named list of tables with metadata).
}
\description{
Reads SNOMED CT UK Monolith Edition RF2 files and adds these
tables to the active codeminer database. This is a convenience wrapper
around \code{\link[=read_snomed_ct_uk_monolith]{read_snomed_ct_uk_monolith()}} that automatically calls
\code{\link[=add_lookup_table]{add_lookup_table()}}, \code{\link[=add_mapping_table]{add_mapping_table()}}, and \code{\link[=add_relationship_table]{add_relationship_table()}}
for each table.

The function processes lookup tables, mapping tables, and relationship
tables from the SNOMED CT release and adds them to the database with their
associated metadata.
}
\details{
The function performs the following steps:
\enumerate{
\item Calls \code{\link[=read_snomed_ct_uk_monolith]{read_snomed_ct_uk_monolith()}} to read the specified tables
\item Iterates through each table and table type (lookup/mapping/relationship)
\item Adds each table to the database using the appropriate \verb{add_*} function
\item Returns the parsed data invisibly
}

This function requires an active database connection set up via
\code{\link[=build_database]{build_database()}} or similar.
}
\examples{
\dontrun{
# Set up database connection
build_database(db_path = "my_codes.db")

# Download latest release and add all tables
add_snomed_ct_uk_monolith()

# Add specific tables only
add_snomed_ct_uk_monolith(
  tables = c("sct_lookup", "sct_icd10")
)

# Use local zip file
snomed_zip <- "~/data/uk_sct2mo_41.3.0_20251217000001Z.zip"
add_snomed_ct_uk_monolith(path = snomed_zip)

# Use already-extracted directory
add_snomed_ct_uk_monolith(
  path = "~/data/SnomedCT_MonolithRF2_PRODUCTION_20251217T120000Z"
)
}

# Example with dummy data (for testing/demonstration)
\dontrun{
build_database(db_path = tempfile(fileext = ".db"))
add_snomed_ct_uk_monolith(
  path = dummy_snomed_ct_uk_monolith_path()
)
}
}
\seealso{
\itemize{
\item \code{\link[=read_snomed_ct_uk_monolith]{read_snomed_ct_uk_monolith()}} for reading without adding to database
\item \code{\link[=add_lookup_table]{add_lookup_table()}}, \code{\link[=add_mapping_table]{add_mapping_table()}}, \code{\link[=add_relationship_table]{add_relationship_table()}}
\item \code{\link[=get_snomed_ct_uk_monolith]{get_snomed_ct_uk_monolith()}} for downloading from NHS TRUD
}
}
