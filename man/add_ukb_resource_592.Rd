% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_ukb_resource_592.R
\name{add_ukb_resource_592}
\alias{add_ukb_resource_592}
\title{Add UK Biobank Resource 592 tables to CodeMiner database}
\usage{
add_ukb_resource_592(
  path = get_ukb_resource_592(),
  sheets = c("bnf_lkp", "dmd_lkp", "icd9_lkp", "icd10_lkp", "icd9_icd10", "read_v2_lkp",
    "read_v2_drugs_lkp", "read_v2_drugs_bnf", "read_v2_icd9", "read_v2_icd10",
    "read_v2_opcs4", "read_v2_read_ctv3", "read_ctv3_lkp", "read_ctv3_icd9",
    "read_ctv3_icd10", "read_ctv3_opcs4", "read_ctv3_read_v2"),
  version = "UKB v4",
  source = "https://biobank.ndph.ox.ac.uk/ukb/refer.cgi?id=592"
)
}
\arguments{
\item{path}{Path to the UKB Resource 592 Excel file. Default uses
\code{\link[=get_ukb_resource_592]{get_ukb_resource_592()}} to download the file if needed.}

\item{sheets}{Character vector of sheet names to read. See
\code{\link[=read_ukb_resource_592]{read_ukb_resource_592()}} for available sheet names. By default, reads all
17 sheets.}

\item{version}{Version label for the UKB resource (default: \code{"UKB v4"}).}

\item{source}{Source URL or description for the UKB resource.}
}
\value{
Invisibly returns the result from \code{\link[=read_ukb_resource_592]{read_ukb_resource_592()}} (a named
list of tables with metadata).
}
\description{
Reads \href{https://biobank.ndph.ox.ac.uk/ukb/refer.cgi?id=592}{UK Biobank Resource 592} and adds these
tables to the active codeminer database. This is a convenience wrapper
around \code{\link[=read_ukb_resource_592]{read_ukb_resource_592()}} that automatically calls
\code{\link[=add_lookup_table]{add_lookup_table()}}, \code{\link[=add_mapping_table]{add_mapping_table()}}, and \code{\link[=add_relationship_table]{add_relationship_table()}}
for each sheet.

The function processes lookup tables, mapping tables, and relationship
tables from the specified sheets and adds them to the database with their
associated metadata.
}
\details{
The function performs the following steps:
\enumerate{
\item Calls \code{\link[=read_ukb_resource_592]{read_ukb_resource_592()}} to read the specified sheets
\item Iterates through each sheet and table type (lookup/mapping/relationship)
\item Adds each table to the database using the appropriate \verb{add_*} function
\item Returns the parsed data invisibly
}

This function requires an active database connection set up via
\code{\link[=build_database]{build_database()}} or similar.
}
\examples{
\dontrun{
# Set up database connection
build_database(db_path = "my_codes.db")

# Add all sheets from UKB Resource 592
add_ukb_resource_592()

# Add specific sheets only
add_ukb_resource_592(
  sheets = c("icd10_lkp", "read_v2_icd10")
)

# Use local file
add_ukb_resource_592(
  path = "path/to/all_lkps_maps_v4.xlsx",
  sheets = "icd9_icd10"
)
}

# Example with dummy data (for testing/demonstration)
\dontrun{
build_database(db_path = tempfile(fileext = ".db"))
add_ukb_resource_592(
  path = dummy_ukb_resource_592_path(),
  sheets = "icd10_lkp"
)
}
}
\seealso{
\itemize{
\item \code{\link[=read_ukb_resource_592]{read_ukb_resource_592()}} for reading without adding to database
\item \code{\link[=add_lookup_table]{add_lookup_table()}}, \code{\link[=add_mapping_table]{add_mapping_table()}}, \code{\link[=add_relationship_table]{add_relationship_table()}}
\item \code{\link[=get_ukb_resource_592]{get_ukb_resource_592()}} for downloading the file
}
}
