% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database-write-lookup.R
\name{lookup_metadata}
\alias{lookup_metadata}
\title{Create lookup metadata}
\usage{
lookup_metadata(
  code_type,
  lookup_version = "v0",
  ...,
  lookup_code_col = "code",
  lookup_description_col = "description",
  lookup_category_col = NA_character_,
  lookup_source = NA_character_,
  preferred_description_col = NA_character_,
  preferred_description_indicator = NA_character_,
  col_filters = NULL
)
}
\arguments{
\item{code_type}{The type of coding system (e.g., ICD-10, SNOMED-CT)}

\item{lookup_version}{The version of the lookup metadata (default: "v0")}

\item{...}{These dots are for future extensions and must be empty.}

\item{lookup_code_col}{The column name for the lookup code (default: "code")}

\item{lookup_description_col}{The column name for the lookup description (default: "description")}

\item{lookup_category_col}{The column name carrying a per-code semantic
category (e.g. ICD-10 chapter, SNOMED FSN class, BNF chapter). Surfaced as
\code{category} by \code{\link[=get_lookup_table]{get_lookup_table()}} for use by hierarchy / tree-rendering
tooling. Default \code{NA_character_}, in which case the \code{category} column is
populated with \code{NA}.}

\item{lookup_source}{The source of the lookup metadata (default: \code{NA_character_})}

\item{preferred_description_col}{The name of the column that indicates the
preferred description. This is useful for lookup tables that have multiple
descriptions for the same code (default: \code{NA_character_}).}

\item{preferred_description_indicator}{The value in the
\code{preferred_description_col} column that indicates the preferred description
(default: \code{NA_character_})}

\item{col_filters}{Optional column filter specification. A named list where
each element is a list with \code{values} (all valid values) and \code{defaults}
(default filter values). See \strong{Details} for the format. \code{NULL} (default)
means no column filters.}
}
\value{
A list containing the lookup metadata
}
\description{
Generate the required metadata for a lookup table. This is mainly used to
generate the necessary metadata when adding a new lookup table to the database
with \code{\link[=add_lookup_table]{add_lookup_table()}}.
}
\details{
The \code{col_filters} argument specifies which columns in the lookup table are
filterable and what the default filter values are. The format is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{list(
  column_name = list(values = c("val1", "val2"), defaults = c("val1"))
)
}\if{html}{\out{</div>}}

When \code{col_filters} is set, query functions like \code{\link[=CODES]{CODES()}} will automatically
filter the lookup table to only include rows matching the default values.
}
\examples{
lookup_metadata("ICD-10", lookup_version = "2023")
}
\seealso{
\code{\link[=add_lookup_table]{add_lookup_table()}}
}
