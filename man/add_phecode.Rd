% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_phecode.R
\name{add_phecode}
\alias{add_phecode}
\title{Add Phecode 1.2 tables to CodeMiner database}
\usage{
add_phecode(
  lkp_path = NULL,
  icd10_map_path = NULL,
  icd9_map_path = NULL,
  version = "v1.2",
  source = "https://phewascatalog.org/phecodes"
)
}
\arguments{
\item{lkp_path}{Path to the Phecode 1.2 definitions CSV. If \code{NULL}, skipped.}

\item{icd10_map_path}{Path to the Phecode 1.2 to ICD-10 mapping CSV. If
\code{NULL}, skipped.}

\item{icd9_map_path}{Path to the Phecode 1.2 to ICD-9 mapping \code{.rda} file.
If \code{NULL}, skipped.}

\item{version}{Character string for the version label (default: \code{"v1.2"}).}

\item{source}{Character string for the data source URL or description.}
}
\value{
Invisibly returns the result from \code{\link[=read_phecode]{read_phecode()}} (a named list of
tables with metadata).
}
\description{
Reads Phecode 1.2 definitions and/or ICD mapping files and adds them to the
active CodeMiner database. This is a convenience wrapper around
\code{\link[=read_phecode]{read_phecode()}} that automatically calls \code{\link[=add_lookup_table]{add_lookup_table()}} and
\code{\link[=add_mapping_table]{add_mapping_table()}} for each table.
}
\examples{
\dontrun{
build_database(db_path = "my_codes.db")

# Download and add all phecode tables
paths <- get_phecode()
add_phecode(
  lkp_path = paths$lkp,
  icd10_map_path = paths$icd10_map,
  icd9_map_path = paths$icd9_map
)
}

# Example with dummy data
\dontrun{
build_database(db_path = tempfile(fileext = ".db"))
add_phecode(
  lkp_path = dummy_phecode_lkp_path(),
  icd10_map_path = dummy_icd10_phecode_map_path()
)
}
}
\seealso{
\code{\link[=read_phecode]{read_phecode()}}, \code{\link[=get_phecode]{get_phecode()}}
}
