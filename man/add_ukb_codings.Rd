% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_ukb_codings.R
\name{add_ukb_codings}
\alias{add_ukb_codings}
\title{Add UK Biobank codings tables to CodeMiner database}
\usage{
add_ukb_codings(
  path = get_ukb_codings(),
  tables = c("opcs4_lkp", "data_coding_3", "data_coding_4", "data_coding_5",
    "data_coding_6"),
  version = "v0",
  source = "https://biobank.ctsu.ox.ac.uk/crystal/exinfo.cgi?src=accessing_data_guide"
)
}
\arguments{
\item{path}{Path to the \code{Codings.tsv} file. Default uses \code{\link[=get_ukb_codings]{get_ukb_codings()}}
to download the file.}

\item{tables}{Character vector of table names to add. See
\code{\link[=read_ukb_codings]{read_ukb_codings()}} for available table names. By default, adds all five
tables.}

\item{version}{Character string for the version label (default: \code{"v0"}).}

\item{source}{Character string for the data source URL or description.}
}
\value{
Invisibly returns the result from \code{\link[=read_ukb_codings]{read_ukb_codings()}} (a named
list of tables with metadata).
}
\description{
Reads the UK Biobank \code{Codings.tsv} file and adds lookup tables for OPCS-4
and UKB self-reported code types to the active CodeMiner database. This is a
convenience wrapper around \code{\link[=read_ukb_codings]{read_ukb_codings()}} that automatically calls
\code{\link[=add_lookup_table]{add_lookup_table()}} for each table.
}
\examples{
\dontrun{
build_database(db_path = "my_codes.db")
add_ukb_codings()
add_ukb_codings(tables = c("opcs4_lkp", "data_coding_4"))
}

# Example with dummy data
\dontrun{
build_database(db_path = tempfile(fileext = ".db"))
add_ukb_codings(
  path = dummy_ukb_codings_path(),
  tables = "opcs4_lkp"
)
}
}
\seealso{
\code{\link[=read_ukb_codings]{read_ukb_codings()}}, \code{\link[=get_ukb_codings]{get_ukb_codings()}}
}
