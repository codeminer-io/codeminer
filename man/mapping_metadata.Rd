% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database-write-mapping.R
\name{mapping_metadata}
\alias{mapping_metadata}
\title{Create mapping metadata}
\usage{
mapping_metadata(
  from_code_type,
  to_code_type,
  map_version = "v0",
  ...,
  from_col = "from",
  to_col = "to",
  map_source = NA_character_,
  col_filters = NULL
)
}
\arguments{
\item{from_code_type}{The type of coding system for the source codes (e.g.,
ICD-10, SNOMED-CT)}

\item{to_code_type}{The type of coding system for the target codes (e.g.,
ICD-10, SNOMED-CT)}

\item{map_version}{The version of the mapping metadata (default: "v0")}

\item{...}{These dots are for future extensions and must be empty.}

\item{from_col}{The column name for the source codes (default: "from")}

\item{to_col}{The column name for the target codes (default: "to")}

\item{map_source}{The source of the lookup metadata (default:
\code{NA_character_})}

\item{col_filters}{Optional column filter specification. A named list where
each element is a list with \code{values} (all valid values) and \code{defaults}
(default filter values), plus the optional \code{description} (single string)
and \code{value_labels} (named character vector, names a subset of \code{values})
documentation fields. See \code{\link[=lookup_metadata]{lookup_metadata()}} for the full format. \code{NULL}
(default) means no column filters.}
}
\value{
A list containing the mapping metadata
}
\description{
Generate the required metadata for a mapping table. This is mainly used to
generate the necessary metadata when adding a new mapping table to the
database with \code{\link[=add_mapping_table]{add_mapping_table()}}.
}
\examples{
mapping_metadata("ICD-10", "SNOMED-CT", map_version = "2023")
}
\seealso{
\code{\link[=add_mapping_table]{add_mapping_table()}}
}
