% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{conditions}
\alias{conditions}
\alias{codeminer_abort}
\alias{codeminer_warn}
\alias{codeminer_inform}
\title{CodeMiner condition constructors \emph{(internal helpers)}}
\usage{
codeminer_abort(
  message,
  class = NULL,
  ...,
  call = rlang::caller_env(),
  .envir = rlang::caller_env()
)

codeminer_warn(message, class = NULL, ..., .envir = rlang::caller_env())

codeminer_inform(message, class = NULL, ..., .envir = rlang::caller_env())
}
\arguments{
\item{message}{It is formatted via a call to \code{\link[cli:cli_bullets]{cli_bullets()}}.}

\item{class}{Optional character vector of additional classes to prepend
before \code{codeminer_error}.}

\item{...}{Passed through to \code{\link[cli:cli_abort]{cli::cli_abort()}}.}

\item{call}{Call environment (only used by \code{\link[=codeminer_abort]{codeminer_abort()}}).}

\item{.envir}{Environment to evaluate the glue expressions in.}
}
\description{
Internal helpers for constructing structured CodeMiner error, warning, and
informational conditions.
}
\details{
These wrap the corresponding \code{cli} signalling
functions (\code{\link[cli:cli_abort]{cli::cli_abort()}}, \code{\link[cli:cli_abort]{cli::cli_warn()}}, \code{\link[cli:cli_abort]{cli::cli_inform()}}) and
always append a CodeMiner-specific base class (\code{codeminer_error},
\code{codeminer_warning}, \code{codeminer_message}).

Additional custom classes may be optionally prepended via the
\code{class} argument.

The original named \code{cli} message vector is stored in \code{cli_message} (see examples).
}
\examples{
# Capture a CodeMiner error condition and inspect it
invalid_code_type <- "foo"

named_cli_message_vector <- c(
  x = "Code type {.arg {invalid_code_type}} not found.",
  i = "Use `add_lookup_table()`."
)

e <- tryCatch(
  codeminer_abort(
    named_cli_message_vector,
    class = "codeminer_arg_validation_error"
  ),
  error = function(cnd) cnd
)

# Inspect condition class hierarchy
class(e)

# Inspect stored structured `cli` message
e$cli_message

# Inspect the default formatted condition message
conditionMessage(e)

# This can be re-thrown to provide an identical error
f <- tryCatch(cli::cli_abort(e$cli_message), error = function(cnd) cnd)

identical(conditionMessage(f), conditionMessage(e))
}
\keyword{internal}
