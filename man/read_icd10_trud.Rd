% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_icd10_trud.R
\name{read_icd10_trud}
\alias{read_icd10_trud}
\title{Read ICD-10 coding files into R}
\usage{
read_icd10_trud(
  path,
  version = NULL,
  source = "https://isd.digital.nhs.uk/trud/"
)
}
\arguments{
\item{path}{Path to the ICD-10 release. Can be:
\itemize{
\item A \strong{zip file} (e.g., from \code{\link[=get_icd10_trud]{get_icd10_trud()}})
\item An \strong{unzipped directory} containing the \code{Content} subdirectory
}}

\item{version}{Character string for the version label. If \code{NULL} (default),
derived from the zip file or directory name.}

\item{source}{Character string for the data source URL or description.}
}
\value{
A named list with elements:
\itemize{
\item \code{icd10_lkp}: lookup table with \code{table} (data.table) and \code{metadata}
\item \code{icd10_relationship}: parent-child hierarchy derived from code structure
}

The lookup \code{table} has columns including \code{CODE}, \code{ALT_CODE} (dot-stripped,
used as the primary code column), \code{DESCRIPTION}, and others from the source
file. \code{DESCRIPTION} is augmented with \code{MODIFIER_4} or \code{MODIFIER_5} where
present. The trailing \code{X} placeholder used to pad 3-char categories to a
4-char width is stripped from \code{ALT_CODE} (e.g., \code{J46X} becomes \code{J46}); the
original \code{CODE} column is left unchanged.
}
\description{
Reads the ICD-10 Edition 5 codes and descriptions from a local copy of the
TRUD release files.
}
\examples{
\dontrun{
path <- get_icd10_trud()
result <- read_icd10_trud(path)
result$icd10_lkp$lookup$table
}
}
\seealso{
\code{\link[=add_icd10_trud]{add_icd10_trud()}}, \code{\link[=get_icd10_trud]{get_icd10_trud()}}
}
