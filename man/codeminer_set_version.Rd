% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codeminer-connect.R
\name{codeminer_set_version}
\alias{codeminer_set_version}
\title{Pin table versions for the session}
\usage{
codeminer_set_version(lookup = NULL, relationship = NULL, mapping = NULL)
}
\arguments{
\item{lookup}{Named character vector of lookup versions, keyed by code type.
E.g. \code{c("ICD-10" = "v42", "Read 3" = "v1")}.}

\item{relationship}{Named character vector of relationship versions, keyed
by code type.}

\item{mapping}{Named character vector of mapping versions, keyed by
\code{"from > to"} pairs. E.g. \code{c("Read 3 > ICD-10" = "v1")}.}
}
\value{
The current pinned versions (a list), invisibly.
}
\description{
Overrides the default "latest" version resolution for lookup, relationship,
and/or mapping tables. Pinned versions persist until cleared with
\code{\link[=codeminer_clear_versions]{codeminer_clear_versions()}} or \code{\link[=codeminer_disconnect]{codeminer_disconnect()}}.
}
\details{
Pinned versions only affect "latest" resolution. Explicit version arguments
on query functions (e.g. \code{CODES(..., lookup_version = "v1")}) always take
precedence.

New pins are merged with existing ones. To replace all pins, call
\code{\link[=codeminer_clear_versions]{codeminer_clear_versions()}} first.
}
\examples{
\dontrun{
# Pin lookup versions for multiple code types
codeminer_set_version(
  lookup = c("ICD-10" = "v42", "Read 3" = "v1")
)

# Pin mapping version for a specific pair
codeminer_set_version(
  mapping = c("Read 3 > ICD-10" = "v1")
)

# Clear all pins
codeminer_clear_versions()
}
}
\seealso{
Other Workbench management: 
\code{\link{codeminer_clear_versions}()},
\code{\link{codeminer_connect}()},
\code{\link{codeminer_disconnect}()},
\code{\link{codeminer_refresh_cache}()},
\code{\link{codeminer_snapshot_extra}()},
\code{\link{codeminer_status}()}
}
\concept{Workbench management}
