% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ukb_codings.R
\name{get_ukb_codings}
\alias{get_ukb_codings}
\title{Download the UK Biobank codings file}
\usage{
get_ukb_codings(dir_path = tempdir(), overwrite = FALSE, quiet = FALSE)
}
\arguments{
\item{dir_path}{Directory to download the file to. Will error if it does not
exist. Defaults to \code{tempdir()}.}

\item{overwrite}{Logical. If \code{TRUE}, re-downloads the file even if it
already exists. Default is \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses informational messages. Default
is \code{FALSE}.}
}
\value{
File path to the downloaded \code{Codings.tsv} (invisibly).
}
\description{
Downloads the UK Biobank \code{Codings.tsv} file, which contains lookup tables
for OPCS-4 and UKB self-reported condition/medication/operation codes.
}
\examples{
\dontrun{
path <- get_ukb_codings()
path <- get_ukb_codings(dir_path = "~/ukb_data", overwrite = TRUE)
}
}
\seealso{
\code{\link[=read_ukb_codings]{read_ukb_codings()}}, \code{\link[=add_ukb_codings]{add_ukb_codings()}}
}
