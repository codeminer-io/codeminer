% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_pseudobnf.R
\name{get_pseudobnf}
\alias{get_pseudobnf}
\title{Get NHS BSA BNF code information (pseudo-BNF)}
\usage{
get_pseudobnf(
  dir_path = tempdir(),
  release = "latest",
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dir_path}{Directory path to download to. Defaults to \code{tempdir()}.}

\item{release}{Character string specifying which release to download. Either:
\itemize{
\item \code{"latest"} (default) — the most recent monthly release.
\item A substring matching a single resource name (e.g. a \code{"YYYYMM"} year-month
or a \code{"version_NN"} label).
}}

\item{overwrite}{Logical. If \code{TRUE}, re-downloads and overwrites an existing
file. Default is \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses informational messages. Default
is \code{FALSE}.}
}
\value{
File path to the downloaded CSV (invisibly).
}
\description{
Downloads the NHS Business Services Authority (NHSBSA) "BNF Code Information"
dataset from the NHSBSA Open Data Portal. This is the full BNF code hierarchy
(chapter -> section -> paragraph -> ... -> presentation), commonly referred to
as the pseudo-BNF classification, published monthly.
}
\details{
Requires the \pkg{nhsbsa} package, which provides a low-level client for the
NHSBSA Open Data Portal API.
}
\examples{
\dontrun{
path <- get_pseudobnf()
read_pseudobnf(path)
}
}
\seealso{
\code{\link[=read_pseudobnf]{read_pseudobnf()}}, \code{\link[=add_pseudobnf]{add_pseudobnf()}}
}
