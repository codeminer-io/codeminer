% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_pseudobnf.R
\name{read_pseudobnf}
\alias{read_pseudobnf}
\title{Read NHS BSA BNF code information (pseudo-BNF) into lookup and relationship tables}
\usage{
read_pseudobnf(
  path = get_pseudobnf(),
  version = NULL,
  source = "https://opendata.nhsbsa.net/dataset/bnf-code-information-current-year"
)
}
\arguments{
\item{path}{Path to the NHS BSA BNF code information CSV. Default uses
\code{\link[=get_pseudobnf]{get_pseudobnf()}} to download the latest release.}

\item{version}{Character string for the version label. If \code{NULL} (default),
derived from the file name.}

\item{source}{Character string for the data source URL or description.}
}
\value{
A named list with element \code{bnf_lkp}, containing:
\itemize{
\item \code{lookup}: a list with \code{table} (data frame) and \code{metadata} (list)
\item \code{relationship}: a list with \code{table} (data frame) and \code{metadata} (list)
}
}
\description{
Reads the NHS Business Services Authority (NHSBSA) "BNF Code Information" CSV
(the full BNF code hierarchy, commonly called the pseudo-BNF classification)
and returns a BNF lookup table plus a parent-child relationship table for use
with CodeMiner.
}
\details{
The lookup mirrors the shape of the BNF lookup historically derived from UK
Biobank resource 592 (one row per code at each level of the BNF hierarchy,
with higher-level name columns populated and deeper ones \code{NA}), so it is a
drop-in replacement. Unlike that approach, each level's code is taken directly
from the portal's explicit \verb{*_CODE} columns rather than sliced from the
presentation code.
}
\examples{
\dontrun{
path <- get_pseudobnf()
result <- read_pseudobnf(path)
result$bnf_lkp$lookup$table
result$bnf_lkp$relationship$table
}
}
\seealso{
\code{\link[=add_pseudobnf]{add_pseudobnf()}}, \code{\link[=get_pseudobnf]{get_pseudobnf()}}
}
