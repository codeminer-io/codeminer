% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database-write-lookup.R
\name{add_lookup_table}
\alias{add_lookup_table}
\title{Add a lookup table to the database}
\usage{
add_lookup_table(table, metadata)
}
\arguments{
\item{table}{The lookup table to add, should be coercible to a \code{data.frame}}

\item{metadata}{The lookup metadata, as specified by \code{\link[=lookup_metadata]{lookup_metadata()}}.}
}
\value{
\code{TRUE} invisibly if successful, \code{FALSE} invisibly if the lookup table
already exists.
}
\description{
Add a lookup table to the database together with its metadata.
Note that it is not possible to overwrite an existing lookup table.
}
\details{
Lookup tables are indexed by their \code{coding_type} and \code{version}, specified in
\code{\link[=lookup_metadata]{lookup_metadata()}}. This index needs to be unique and is used to identify
the lookup table in the database. If a lookup table with the same
\code{coding_type} and \code{version} already exists, the function will emit a warning
and return \code{FALSE} (invisibly) without any effect. Use a different \code{version}
to add a new version of the lookup table for the given \code{coding_type}.
}
\examples{
# Using the example ontology data included in codeminer
lookup_table <- example_ontology$lookup_tables$capital_letters_v3
lookup_table

# Using a temporary database
Sys.setenv(CODEMINER_DB_PATH = tempfile())
build_database()
add_lookup_table(lookup_table, lookup_metadata("capital_letters", version = "v3"))
}
\seealso{
\code{\link[=lookup_metadata]{lookup_metadata()}} for the specification of the metadata.
}
